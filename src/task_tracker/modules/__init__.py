"""Feature modules.

Each full-stack feature should usually live in its own package under here:
- `api.py` for HTTP routes
- `schemas.py` for request/response contracts
- `service.py` for business logic
- `repository.py` for SQL/data access
- `schema.py` for table initialization

Use `tasks` as the concrete example and `_template` as the copy/paste starter.
"""
